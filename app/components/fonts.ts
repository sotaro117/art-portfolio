import { Inter, Lusitana, Inconsolata } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const lusitana = Lusitana({
  weight: ["400", "700"],
  subsets: ["latin"],
});
export const inconsolata = Inconsolata({
  weight: "600",
  subsets: ["latin"],
});
